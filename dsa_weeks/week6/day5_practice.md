# Day 5: Week 6 Practice

**Time:** ~45 minutes

Do not use `min()`, `max()`, `.index()`, or `.sort()` unless a challenge says
you may.

**Practice file:** Create `day5_practice.py`

---

## Challenge 1: Minimum and Maximum

Return both the smallest and largest values using one loop.

```python
min_max([4, 2, 9, 1, 7])  # (1, 9)
```

## Challenge 2: Running Total

Build a list of prefix sums.

```python
running_totals([2, 3, 1, 4])  # [2, 5, 6, 10]
```

## Challenge 3: First and Last Position

Return the first and last index of a target.

```python
positions([1, 4, 2, 4, 4, 8], 4)  # (1, 4)
```

## Challenge 4: Binary Search

Implement binary search without looking at Day 4.

## Challenge 5: Complexity Check

Write the time and space complexity beneath every function.

## Graduation Check

Move to Week 7 when you can explain:

- Why one loop is usually `O(n)`
- Why nested loops are often `O(n²)`
- Why binary search is `O(log n)`
- Why inserting at the front of a list is expensive

